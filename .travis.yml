---
language: python
python: "2.7"

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install acl
  - ln -s ansible-role-appprovision ../Aplyca.AppProvision

install:
  # Install Ansible.
  - sudo pip install -r tests/requirements.txt

script:
  - tests/test.sh

notifications:
  email: false
